إليك وثيقة التوثيق الفني الشاملة والمصممة خصيصاً لتكون الملف الرئيسي **`README.md`** في مستودع مشروعك على **GitHub**. تم كتابة هذه الوثيقة بأسلوب هندسي دقيق ومفصل جداً خالٍ من أي اختصارات، لتشرح البنية الهيكلية للمشروع، ومخططات قواعد البيانات الجغرافية، والتحسينات التشغيلية والأمنية التي قمنا بدمجها، بالإضافة إلى دليل كامل لتثبيت وتشغيل النظام من الصفر:

---

# 📑 دليل التشغيل والأرشيف الهندسي لمنصة GIS MANAGER السحابية

منصة **GIS MANAGER** هي نظام جيو-معلوماتي سحابي متكامل ومطور (Geospatial Records Management System) لإدارة وتوثيق المعاينات الميدانية وتتبع الأنشطة الهندسية والمخالفات الجغرافية. تم بناء وتطوير المنصة بالاعتماد على معايير البرمجة الإجرائية النظيفة (Clean Procedural PHP) وهيكلية موجهة مرنة ومستقلة، مما يمنحها قوة معالجة فائقة، حماية أمنية متعددة المستويات، واستقراراً بطلاقة تامة مع جميع الهواتف الذكية وأجهزة الكمبيوتر عبر تقنيات PWA.

---

## 📂 أولاً: المعمارية الهيكلية للملفات (Project Directory Structure)

يعتمد النظام على هيكلية **"الموجّه المركزي المفصول" (Decoupled Router Layout)**؛ حيث تم الفصل التام برمجياً وهيكلياً بين موديولات النظام لتقليل حجم الملفات وتفادي تداخل الاستعلامات وسرعة تحميل الواجهات.

```text
public_html/msgisvip/
├── config.php                 # ضبط الجلسة الأمنية وحمايتها، مفاتيح reCAPTCHA، وتوليد CSRF Token آلياً
├── db.php                     # الاتصال بقاعدة البيانات PDO، دوال الرقابة، التنبيهات، والتحقق من CSRF
├── login.php                  # واجهة تسجيل الدخول المؤمنة بـ reCAPTCHA v2 من جوجل
├── logout.php                 # إنهاء الجلسة الأمنية وتصفير كوكيز المتصفح والتحويل الآمن
├── index.php                  # الموجه المركزي والتحكم بسلايدبار الهاتف التفاعلي واعتراض طلبات التصدير
├── print.php                  # محرك ومستند الطباعة المعتمد المنسق للتقارير الورقية A4
├── manifest.json              # ملف تهيئة الـ PWA لتمكين تثبيت المنصة كتطبيق جوال
├── sw.js                      # سكريبت الخدمة (Service Worker) الخامل المطور لتحديث وحماية كاش الهواتف
├── includes/                  # الأجزاء البصرية المشتركة لجميع الصفحات
│   ├── header.php             # رأس الصفحة (HTML Head) وتحميل Tailwind و Leaflet و Geoman
│   └── sidebar.php            # القائمة الجانبية (الأزرار والصفحات) المفردة حسب تصاريح الموظف
└── pages/                     # موديولات وصفحات النظام المستقلة كلياً
    ├── home-view.php          # بوابة النظام الرئيسية (لوحة إطلاق مركزية تفاعلية شاملة لجميع الأقسام)
    ├── add-record.php         # نموذج التوثيق الميداني والالتقاء الجغرافي التلقائي مع مضلعات KML
    ├── records-manage.php     # إدارة وجرد السجلات وتخصيص الأعمدة مع تفعيل الـ Pagination والعمليات الجماعية
    ├── map-view.php           # الخريطة التفاعلية، تجميع الأيقونات، ورسم الحدود الجغرافية وحفظها بـ Base64
    ├── reports-view.php       # منشئ التقارير التفصيلية، الفرز بالأعمدة، والتقسيم الذكي لصفحات المعالجة
    ├── dashboard-view.php     # لوحة المؤشرات البيانية المصفاة وباني الـ KPIs المخصص (KPIs Dashboard)
    ├── import-view.php        # مركز الاستيراد الميداني وتوليد قوالب إكسل المجهزة بالـ Data Validation
    ├── export-view.php        # مركز تصدير التقارير الجداول وخرائط الـ KML المصفاة تاريخياً وفنياً
    ├── transfers-view.php     # مركز إدارة الصادر والوارد وحركة السجلات وحيازتها الإدارية
    ├── settings-view.php      # إعدادات النظام الفنية وباني الحقول المشتركة والنشطة
    ├── print-settings.php     # مصمم قوالب وتوقيعات الطباعة الرسمية وهيدر الترويسة والشعار
    ├── users-view.php         # إدارة الموظفين وتصاريح الصفحات والأقسام وإعادة التوجيه الآمن بعد الـ POST
    ├── view-record.php        # استعراض تفاصيل المعاينة والخريطة المصغرة
    ├── edit-record.php        # تعديل واستبدال معطيات السجل والمرفقات الفنية
    └── export-csv.php         # التصدير اللامركزي لـ Excel متكامل مع فلاتر الأعمدة والـ JSON
```

---

## 🗄️ ثانياً: هندسة قاعدة البيانات والأعمدة المولدة الذكية (Database Schema)

لتحقيق المرونة المطلقة دون تعديل بنية الجداول هيكلياً عند إضافة حقول جديدة؛ يعتمد النظام على مصفوفة **(Metadata-Driven Database)**؛ حيث تُخزن البيانات المشتركة في أعمدة منفصلة (لتسريع الفرز والبحث الجغرافي)، وتُخزن البيانات المتغيرة المخصصة في عمود واحد من نوع **JSON** يتم تفكيكه ومعالجته برمجياً.

تم إدخال تعديلات هندسية على قاعدة البيانات لرفع كفاءة تصفح وجلب البيانات:

### 1. جدول السجلات الرئيسي (`records`)
يخزن المعاينات الميدانية؛ وتم تزويده بفهارس جغرافية وأعمدة استخلاص مخزنة لسرعة الاستعلام:
*   `geom` (POINT): حقل جيو-مكاني من نوع `POINT` يتم توليده وتحديثه تلقائياً بصيغة (Stored Virtual) بناءً على خطوط الطول والعرض المدخلة لتمكين الفهرسة الجغرافية السريعة.
*   `extracted_point_number` & `extracted_owner_name`: حقول افتراضية مخزنة مفهرسة تستخلص البيانات من الـ JSON تلقائياً لتسريع البحث النصي والجرد بصفحة التقارير.
*   `idx_records_type_id` & `idx_records_user_id`: فهارس مخصصة على حقول الربط لتسريع عمليات الـ JOIN.

### 2. جدول الحركة المستندية والصادر والوارد الجديد (`record_transfers`)
يخزن السجلات التاريخية لحركة السجلات والحيازة الإدارية بين الأقسام:
*   `record_ids` (TEXT): يحمل أرقام المعاينات المشمولة بالتحويل كأرقام نصية مفصولة بفاصلة (مثال: `"21,22,23"`).
*   `sender_dept`: الإدارة الصادر منها (المرسلة).
*   `receiver_dept`: الإدارة الوارد إليها (المستلمة).
*   `status`: حالة المعاملة: معلقة (`pending`) لانتظار الاستلام، ومستلمة ومكتملة (`approved`).

### 3. جدول أنشطة النظام الرقابي (`audit_logs`)
تم تزويده بفهارس مخصصة لـ `user_id` و `created_at` لتسريع تحميل لوحة الرقابة التفاعلية وتصفيتها بالثانية.

---

## 💻 ثالثاً: التفصيل التشغيلي للموديولات والوظائف المطورة (System Modules)

### 1. بوابة النظام لوحة الإطلاق المركزية (`home-view.php`)
تم تطويرها لتصبح بوابة نظام ذكية؛ تقرأ صلاحيات المستخدم آلياً وتعرض له كروت تفاعلية ملونة لجميع الموديولات والخدمات الـ 14 المصرح له بدخولها فقط بناءً على رتبته الإدارية، مع حجب الكروت الإدارية كلياً عن الموظفين الميدانيين لحماية إعدادات النظام.

### 2. معالج الاستيراد وتوطين القوالب المعتمدة (`import-view.php`)
تم فصله وتطويره بالكامل ليعمل بأداء خفيف ومظهر منقسم لبوكسات عصرية:
*   **منع التكرار ثنائي الحقول (Dual-Field Deduplication):** يتيح للمشرف تحديد حقلين وقائيين من حقول النظام (مثل رقم النقطة ورقم المعاملة) للتحقق المتبادل منهما. يقوم السيرفر بفحص حقل الـ JSON مباشرة لضمان عدم وجود قيمة مطابقة مسبقاً، وفي حال وجودها يتم تخطي الصف المكرر وتوثيقه في عداد السجلات المتخطاة.
*   **توليد قوالب إكسل مدعمة بالـ Data Validation:** عند تنزيل قالب الاستيراد المخصص للقسم، يقوم السيرفر آلياً بتحويل أرقام حقول قاعدة البيانات إلى حروف الأعمدة المقابلة لها في إكسل، ويحقن تاجات المواءمة القياسية لمايكروسوفت إكسل (`<x:DataValidation>`) بداخل مخطط ملف الـ XLS المولد باستخدام الفاصلة المنقوطة (`;`) المتوافقة مع إكسل والويندوز العربي لضمان تطابق البيانات المدخلة ومنع الأخطاء الإملائية.
*   **معالجة تواريخ إكسل الرقمية (Excel Serial Dates):** يشتمل الملف في أعلاه على دالة حاسبة ذكية تقوم تلقائياً بتحويل الأرقام التعاقبية لملفات إكسل (مثل `46023`) إلى تاريخ مقروء وصريح بالصيغة القياسية (`YYYY-MM-DD`) قبل حفظها في قاعدة البيانات.
*   **محرك المطابقة اللغوية والبرمجية التلقائي (Semantic Mapping Engine):** بمجرد اختيار القسم، يقوم كود جافا سكريبت بمطابقة أسماء أعمدة ملفك المرفوع ومواءمتها تلقائياً مع حقول النظام وتلوين الكروت المواءمة بالأخضر.
*   **شيت التدقيق والتحقق الذكي الفوري (Smart Auditing Spreadsheet):** يوفر جدول مراجعة يدوي مساعد منسق بالكامل يعرض كافة الصفوف والمدخلات كحقول نصية وخيارات منسدلة مخصصة مطابقة تماماً لنوع الحقول المسجلة بالنظام لتعديلها وحفظها على الشاشة مباشرة. تم تزويده بـ (مكتشف ومصحح الأخطاء التلقائي)؛ حيث يقوم بفحص الخلايا ومطابقتها وتلوينها باللون الأحمر مع شارة "مطلوب" أو البرتقالي مع شارة "غير صالح للإحداثيات" عند وجود أي خطأ، مع شريط حالة تفاعلي يعطل زر الحفظ حتى يتم تصحيح الأخطاء.

### 3. مركز الصادر والوارد وتتبع الحركة المستندية (`transfers-view.php`)
موديول تفاعلي جديد ينظم سلسلة الحيازة للسجلات بين الإدارات:
*   **التصدير وصنع الصادر:** يتيح للمشرف تحديد عدة سجلات في لوحة الإدارة وتصديرها كشحنة واحدة وتحديد الإدارة المستلمة وملاحظات التسليم، لتتغير حالة السجلات تلقائياً بالنظام إلى: **"صادر معلق إلى [الجهة المستلمة]"**.
*   **التأكيد والاستلام التلقائي للوارد:** عند دخول موظف الجهة المستلمة، تظهر له تنبيهات الشحنات المعلقة. بمجرد ضغطه على زر "تأكيد الاستلام"؛ يتم تحديث حالة السجلات تلقائياً بجدول الـ JSON لتصبح: **"مستلمة وفي حوزة [الإدارة الجديدة]"** وتوثق العملية في سجل الرقابة بالتوقيت والموظف المستلم بدقة تامة.
*   **البحث والجرد التاريخي:** تم تزويد الموديول بمحرك بحث متطور وجدول مؤرشف يتيح للمدير الفرز ومعرفة كافة السجلات التي نُقلت لإدارة معينة خلال فترة زمنية محددة بدقة.

### 4. نموذج التوثيق والالتقاء الجغرافي التلقائي (`add-record.php`)
*   **منع التداخل والتكرار الجغرافي الهافيرسيني (Proximity Prevention):** تم دمج الاستعلام الجغرافي السريع المعتمد على الدالة المدمجة `ST_Distance_Sphere` مستهدفاً الحقل الجغرافي المطور `geom` للتحقق من وجود أي مخالفة مسبقة في نطاق 30 متراً من موقع الموظف الفعلي وحظر حفظها كمسودة مكررة.
*   **الالتقاء والتحقق الجغرافي التلقائي (Automatic Geofencing):** بمجرد التقاط الموظف للإحداثيات، يرسل المتصفح استعلاماً خلفياً سريعاً (Ajax API) لمطابقة الموقع مع مضلعات الـ KML المخزنة في جدول `boundaries` ويعيد اسم الحي المطابق.
    *   *إذا كان المستخدم (Admin):* تظهر له نافذة تأكيد تفاعلية (SweetAlert) تسأله عن رغبته في اعتماد الحي وتعبئته بالاستمارة تلقائياً أو تعديله يدوياً.
    *   *إذا كان المستخدم (موظف ميداني عادي):* يتم تعبئة حقل المنطقة بالاستمارة تلقائياً مع قفل الحقل (Readonly/Disabled) لمنعه من تغييره أو التلاعب بحدود المخالفة الجغرافية لضمان سلامة وصحة البيانات الميدانية.

### 5. الخريطة التفاعلية ورسم الحدود الإدارية المباشر (`map-view.php`)
*   **تكتيل وتجميع الأيقونات (Marker Clustering):** مدمج بها نظام تجميع ذكي لضغط آلاف النقاط في دوائر متوهجة ديناميكياً لتسريع استجابة الخريطة، مع ميزة الانتشار العنكبوتي عند تطابق الإحداثيات والتقريب التلقائي، وإلغاء التكتيل عند التقريب لمستوى الشارع.
*   **باني ومخطط الحدود الإدارية المباشر (Leaflet Geoman):** تم دمج مكتبة رسم خفيفة ومعربة بالكامل تظهر لمدير النظام (Admin) فقط. عند انتهاء المدير من رسم أي مضلع جغرافي على الخريطة، يتم استدعاء دالة `generateKmlFromLatLngs` التي تقوم بالتقاط النقاط الجغرافية وصياغة مضلع KML مغلق وسليم هندسياً (مرتب بتقديم خط الطول أولاً ثم العرض ليتوافق مع معايير جوجل إيرث). ثم يتم ترميز كود الـ KML المولد بصيغة **`Base64`** وإرساله عبر طلب POST آمن إلى السيرفر لحفظه بجدول `boundaries` وتنشيطه فوراً.

### 6. لوحة إدارة السجلات الميدانية المحدثة (`records-manage.php`)
تم تزويد لوحة الإدارة بـ:
*   **التقسيم البرمجي لصفحات (Server-Side Pagination):** يعرض 20 سجلاً فقط بالصفحة من قاعدة البيانات لضمان الخفة والسرعة العالية بالتحميل، مع شريط تنقل سفلي أنيق يحافظ على فلاتر البحث الحالية أثناء التنقل.
*   **مربعات التحديد والعمليات الجماعية للصادر والوارد:** تم دمج مربعات التحديد (Checkboxes) بجانب كل صف مع شريط عمليات عائم سفلي يظهر عند التحديد لتصدير المعاملات المحددة كشحنة صادر دفعة واحدة.

### 7. لوحة المؤشرات البيانية وباني الـ KPIs المطور (`dashboard-view.php`)
تم تخصيص هذه الصفحة بالكامل لتصبح مركز التحليلات والمؤشرات:
*   **الرسوم البيانية الأساسية المصفاة:** رسوم دائرية لتوزيع السجلات بالأقسام ورسوم خطية لمعدل النمو الشهري تستقبل الفلاتر الزمنية والفنية وتتحدث آلياً.
*   **باني ومصمم الكروت والمؤشرات المخصصة (KPIs):** موديول مستقل يتيح للمشرف تحديد الحقول الفنية المُراد إبراز كروت وإحصائيات تكرارها، ليعرض كرت موحد لكل حقل يحتوي بداخله على الأعداد الإجمالية أو تكرار عناصر القائمة المنسدلة فقط بألوان زاهية وخطوط عريضة مريحة للعين مع دعم طباعة تقارير المؤشرات لـ PDF.

### 8. صيانة وتطهير السجلات وقاعدة البيانات (`backup-view.php`)
*   **الصيانة التلقائية وتطهير الملفات اليتيمة:** تقوم أداة الصيانة الذكية المحدثة بجرد ومطابقة مجلدات المرفقات الفعلية بالسيرفر ومقارنتها بقاعدة البيانات وحذف أي ملف يتيم أو صور ميتة غير مسجلة لتوفير مساحة السيرفر فورياً.
*   **أدوات التطهير والحذف المخصصة للسجلات:** تم دمج ميزة الحذف والتطهير المتقدمة المخصصة، والتي تمكن الإدارة من:
    *   حذف سجلات فترة زمنية محددة، أو حذف سجلات قسم ميداني معين، أو إفراغ ومحو كافة السجلات الميدانية بالكامل (تصفير).
    *   *صمام الأمان التفاعلي لحفظ السجلات:* تم تأمين هذه العمليات الحساسة بطلب كتابة كلمة تأكيدية صريحة وهي كلمة **`DELETE`** أو **`حذف`** في شاشة منبثقة تفاعلية قبل تفعيل زر الحذف لتجنب أي حذف خاطئ.

### 9. القائمة الجانبية المنزلقة المتجاوبة للهواتف (`includes/sidebar.php`)
تمت إعادة تصميم السايدبار بالكامل ليتلاءم مع شاشات الهواتف الذكية:
*   **نظام الدرج المنزلق (Responsive Drawer Sidebar):** في شاشات الهاتف المحمول يتم إخفاء السايدبار بالكامل خارج الشاشة يميناً (`translate-x-full`)؛ وعند النقر على زر التحكم العلوي ينسلق السايدبار كـ درج جانبي فخم يغطي الشاشة مع ظهور خلفية ضبابية مظلمة (Overlay Background) عند النقر عليها يغلق السايدبار تلقائياً، دون مضايقة أو حجب الخريطة وموقع الـ GPS الفعلي.
*   **فرز صلاحيات الموديولات الجديدة:** تم ربط السايدبار ديناميكياً لفرز الأزرار الجديدة (الصادر والوارد، الاستيراد، التصدير) وعرضها بناء على تصاريح الموظف النشطة.

---

## 🔒 رابعاً: التدابير والحماية الأمنية للنظام (Security Architecture)

تم تأسيس البنية الأمنية للمنصة لمنع محاولات الاختراق، حقن البيانات، أو تسريب تفاصيل السيرفر:

1.  **تجاوز اعتراض جدران الحماية الصارمة (WAF Bypass):**
    لحماية مدخلات النظام وتفادي حظر جدران الحماية الخبيث (ModSecurity 403)؛ تم استخدام تقنية **ترميز الـ Base64 لبيانات الاستمارات الحساسة** في طرف العميل (المتصفح) قبل إرسالها (مثل حقول تشييد الإعدادات ونصوص الـ KML المفتوحة للحدود المرسومة يدوياً)، ومن ثم فك ترميزها برمجياً عبر دالة `base64_decode` داخل السيرفر قبل معالجتها وحفظها.
2.  **تأمين وحماية ملفات الكوكيز وجلسات المشرفين (Session Security):**
    تم ضبط معايير أمان ملفات تعريف الارتباط تلقائياً في أعلى ملف `config.php` قبل استدعاء `session_start()` لضمان حماية الجلسات ضد ثغرات الـ XSS وخطف الكوكيز برمجياً عبر تفعيل خيار **`httponly`**، وتفعيل خيار **`samesite => Lax`** لحظر إرسال الكوكيز من الروابط الخارجية، وتشفير حركة البيانات تلقائياً في حال توفر بروتوكول الـ HTTPS الآمن عبر خيار **`secure`**.
3.  **منع تسريب تفاصيل الاتصال البرمجية (Information Disclosure Prevention):**
    تم تأمين كتلة اصطياد أخطاء الاتصال بـ PDO داخل ملف الاتصال `db.php`؛ حيث تم إيقاف طباعة الأخطاء الصريحة للمسؤول وتوجيه الخطأ التفصيلي الحقيقي إلى ملف سجلات الأيرور المغلق بالسيرفر برمجياً عبر دالة `error_log()` لتجنب تسريب اسم مستخدم قاعدة البيانات أو المسارات الداخلية للمنصة.
4.  **منع تداخل وتراكم كاش الهواتف (Passive PWA Service Worker):**
    تمت إعادة هندسة وصياغة ملف الخدمة `sw.js` ليكون خاملاً ومقتصراً على تأمين شرط تثبيت التطبيق على الشاشات، مع برمجته ليقوم **بتدمير وتطهير كاش المتصفحات القديمة للمستخدمين تلقائياً**، لمنع حجب الصلاحيات وتحديث السايدبار وعرض الأزرار الجديدة في جميع صفحات الموقع (بما في ذلك الصفحة الرئيسية) بشكل فوري.

---

## 🛠️ خامساً: دليل تثبيت النظام وتهيئته من الصفر (System Installation)

باتباع هذه الخطوات الهندسية؛ يمكنك تنصيب وتثبيت المنصة وتشغيلها على أي سيرفر سحابي أو استضافة مشتركة (cPanel) بنجاح:

### 1. المتطلبات البرمجية للسيرفر (Prerequisites):
*   نظام تشغيل سحابي (Linux/Windows Server).
*   خادم ويب Apache أو Nginx مجهز ببروتوكول SSL آمن (HTTPS).
*   إصدار PHP 8.1 أو ما يليه.
*   تفعيل الإضافات البرمجية التالية بـ PHP: (PDO, pdo_mysql, ZipArchive, SimpleXML, mbstring).
*   قاعدة بيانات MySQL أو MariaDB إصدار 10.4 فما فوق لدعم ميزات الـ JSON الجغرافية المتقدمة.

### 2. خطوات التثبيت والتهيئة (Step-by-Step Deployment):
1.  **رفع ملفات المشروع:** قم برفع مجلدات وملفات المنصة بالكامل إلى المسار الرئيسي لاستضافتك (`public_html`).
2.  **استيراد قاعدة البيانات:** ادخل إلى لوحة التحكم phpMyAdmin، وأنشئ قاعدة بيانات جديدة بترميز (`utf8mb4_unicode_ci`)، ثم قم باستيراد سكريبت قاعدة البيانات المرفق `gismanger_msgis.sql`.
3.  **إعداد ملف الاتصال السري:**
    *   قم بنسخ الملف الإرشادي `config.example.php` وأعد تسميته إلى **`config.php`**.
    *   افتح الملف واكتب بيانات الاتصال الحقيقية لقاعدة البيانات التي أنشأتها:
        ```php
        define('DB_HOST', 'localhost');
        define('DB_NAME', 'اسم_قاعدة_البيانات');
        define('DB_USER', 'اسم_مستخدم_قاعدة_البيانات');
        define('DB_PASS', 'كلمة_المرور_السرية');
        ```
    *   اكتب مفاتيح Google reCAPTCHA v2 وتوكنات البوت والمعرف الخاص بجروب التليجرام الخاص بك.
4.  **تثبيت وإعداد ملف المزامنة والـ PWA:**
    *   تأكد من وجود ملف الـ `.gitignore` في جذر مشروعك لحماية المرفقات والملفات الحساسة من الرفع على مستودع GitHub.
    *   ادخل إلى المنصة برابط استضافتك المباشر، وسجل الدخول بالحساب الافتراضي للمدير:
        *   اسم المستخدم: `admin`
        *   الباسورد الافتراضي: `Ms@01009036205`
    *   قم بتغيير بيانات الدخول وتعيين باسبورد جديد خاص بك فوراً من صفحة (الملف الشخصي).
```